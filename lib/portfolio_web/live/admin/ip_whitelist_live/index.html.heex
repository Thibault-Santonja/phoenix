<div class="max-w-6xl mx-auto px-4 py-8">
  <div class="flex justify-end items-center mb-6">
    <.link patch={~p"/admin/ip-whitelist/new"}>
      <span class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 inline-block">
        {gettext("admin.ip_whitelist.index.add")}
      </span>
    </.link>
  </div>

  <div class="bg-white shadow-md rounded-lg overflow-hidden">
    <%= if @entries == [] do %>
      <div class="p-8 text-center text-gray-500">
        {gettext("admin.ip_whitelist.index.empty")}
      </div>
    <% else %>
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              {gettext("admin.ip_whitelist.index.column_ip")}
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              {gettext("admin.ip_whitelist.index.column_description")}
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              {gettext("admin.ip_whitelist.index.column_created_by")}
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              {gettext("admin.ip_whitelist.index.column_date")}
            </th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">
              {gettext("admin.ip_whitelist.index.column_actions")}
            </th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <%= for entry <- @entries do %>
            <tr>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                {entry.ip_address}
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                {entry.description || "-"}
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= if entry.created_by do %>
                  {entry.created_by.email}
                <% else %>
                  -
                <% end %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                {Calendar.strftime(entry.inserted_at, "%Y-%m-%d %H:%M")}
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                <.link
                  patch={~p"/admin/ip-whitelist/#{entry.id}/edit"}
                  class="text-blue-600 hover:text-blue-900 mr-4"
                >
                  {gettext("admin.ip_whitelist.index.edit")}
                </.link>
                <button
                  phx-click="delete"
                  phx-value-id={entry.id}
                  data-confirm={gettext("admin.ip_whitelist.index.delete_confirm")}
                  class="text-red-600 hover:text-red-900"
                >
                  {gettext("admin.ip_whitelist.index.delete")}
                </button>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  </div>
</div>

<.modal
  :if={@live_action in [:new, :edit]}
  id="ip-whitelist-modal"
  show
  on_cancel={JS.patch(~p"/admin/ip-whitelist")}
>
  <.live_component
    module={PortfolioWeb.Admin.IPWhitelistLive.FormComponent}
    id={@ip_whitelist.id || :new}
    title={@page_title}
    action={@live_action}
    ip_whitelist={@ip_whitelist}
    current_user={@current_user}
    patch={~p"/admin/ip-whitelist"}
  />
</.modal>
